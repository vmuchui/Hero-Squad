<h1>These heroes belong to $createdSquad.getSquadName() squad.</h1>
#if($heroes)
  #foreach($hero in $heroes)
    <p>Hero's name: $hero.getName()</p>
    <p>Hero's age: $hero.getAge()</p>
    <p>Hero's strength: $hero.getStrength()</p>
    <p>Hero's </p>
  #end
  
#else
  <h3>Create Heroes to add to this squad:</h3>
  <form action="/" method= post>
  </form>
#end